# Configurações do Projeto

Este arquivo contém informações de configuração para o projeto de estudo de SQL com MySQL.

- Para executar os scripts SQL:
  - Certifique-se de ter o MySQL instalado localmente.
  - Use o cliente MySQL no terminal para executar os scripts.

- Estrutura do Projeto:
  - `exemplos/`: Contém exemplos de scripts SQL.
  - `recursos/`: Contém arquivos adicionais de configuração e instruções.

Para executar um script SQL:
1. Clone este repositório em sua máquina local.
2. Navegue até o diretório `exemplos/`.
3. Execute o script desejado utilizando o MySQL:
